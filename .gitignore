node_modules/
dist/
release/
.env
.env.local
*.log
.DS_Store
Thumbs.db

# Local session storage (cookies, MUSIC_U, etc.) — never commit
packages/server/.storage/
.storage/

# Claude Code：只提交共享配置，本地个人权限/覆盖不提交
.claude/settings.local.json

# TypeScript incremental build artifacts
*.tsbuildinfo

# Superdesign temp design artifacts
.superdesign/tmp/
coverage/

# figma-code-connect-inject.mjs 备份（避免污染 git status）
figma-code-connect.json.bak
figma-code-connect.json.bak2
